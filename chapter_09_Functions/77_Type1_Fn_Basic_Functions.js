// Define
function greet() {
    console.log("Hi");
}

// This is a Basic type-1 function, which means no argument, no return. 
// Call
greet();

// calling a type-1 function and assigning it to a variable will give us undefined, because there is no return value.
let a = greet();