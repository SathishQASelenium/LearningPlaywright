// && -> AND Gate
// || -> OR Gate

let a = true;
let b = false;
console.log(`a && b ->`, a && b); //  AND
console.log(`a || b ->`, a || b); //  OR
console.log(`!a ->`, !a); // Reverse of a
console.log(`!b ->`, !b); // Reverse of b